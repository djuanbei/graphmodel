#include "util/datacompression.h"

namespace graphsat {
uint *Compression::encode( int *data ) const { return NULL; }

int *Compression::decode( uint *data ) const { return NULL; }

} // namespace graphsat
