#include "gtest/gtest.h"

#include "model/location.h"

using namespace graphsat;

TEST(__LOCATION_H, LOCATION) {
  // Location loc(1);
  // EXPECT_EQ(loc.getName(), "Loc_1");
  // EXPECT_EQ(loc.getId(), 1);
  // Location loc1(2);
  // EXPECT_EQ(loc1.getId(), 2);
  // EXPECT_FALSE(loc.isUrgent());
  // EXPECT_FALSE(loc.isCommit());
  // EXPECT_FALSE(loc.isFreezeLocation());
}
